export class spaceShip {
  name: string
  fuel: number
  capacity: number

  constructor(name: string, fuel: number, capacity: number) {
    this.name = name
    this.fuel = fuel
    this.capacity = capacity
  }
}
